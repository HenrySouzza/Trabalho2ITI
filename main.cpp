#include <iostream>
#include "filtrar_arquivo.h"
#include "k_1.h"
#include "gerarArvores.h"

using namespace std;

int main() {
    // // Filtrar o arquivo de entrada e salvar o resultado no arquivo de saída
    //  ifstream arquivo_entrada("MemoriasPostumas.txt");
    //  ofstream arquivo_saida("saida.txt");
    //  if (!arquivo_saida || !arquivo_entrada) {
    //      cerr << "Erro ao abrir o arquivo" << endl;
    //      return 1;
    //  }
    //  limpar_texto(arquivo_entrada, arquivo_saida);
    // ////////////////////////////////////////////////////////////////////////

    // // Gerar a árvore de Huffman
    // vector<int> valores= {60, 31, 16, 8, 4, 4};     
    // int posicao = 0;
    // string codigo = obterCodigoHuffman(valores, posicao);
    // cout << "Código de Huffman: " << codigo << endl;
    // ////////////////////////////////////////////////////////////////////////
    
    return 0;
}
